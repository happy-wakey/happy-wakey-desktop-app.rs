// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'env_io_stub.dart' if (dart.library.io) 'env_io.dart' as platform;

final class DesktopEnvValues {
  const DesktopEnvValues({required this.configDir, required this.gatewayUrl, required this.gitRepoPath, required this.happyWakeyOauthPort, required this.openMeteoBaseUrl, required this.platformUrl, required this.sharedAuthUrl, required this.supabaseUrl});

  final String? configDir;
  final String? gatewayUrl;
  final String? gitRepoPath;
  final String? happyWakeyOauthPort;
  final String openMeteoBaseUrl;
  final String? platformUrl;
  final String? sharedAuthUrl;
  final String supabaseUrl;
}

/// Pure: resolve values from an explicit lookup.
DesktopEnvValues loadFrom(String? Function(String key) lookup) {
  return DesktopEnvValues(
    configDir: _nonEmpty(lookup('CONFIG_DIR')),
    gatewayUrl: _nonEmpty(lookup('HAPPY_WAKEY_GATEWAY_URL')),
    gitRepoPath: _nonEmpty(lookup('GIT_REPO_PATH')),
    happyWakeyOauthPort: _nonEmpty(lookup('HAPPY_WAKEY_OAUTH_PORT')),
    openMeteoBaseUrl: _nonEmpty(lookup('OPEN_METEO_BASE_URL')) ?? 'https://api.open-meteo.com/v1/forecast',
    platformUrl: _nonEmpty(lookup('HAPPY_WAKEY_PLATFORM_URL')),
    sharedAuthUrl: _nonEmpty(lookup('HAPPY_WAKEY_SHARED_AUTH_URL')),
    supabaseUrl: _nonEmpty(lookup('SUPABASE_URL')) ?? 'https://vgzyyfhnendriyrhakkp.supabase.co',
  );
}

/// Resolve values from a map of env keys.
DesktopEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
DesktopEnvValues loadFromOs() {
  return loadFromMap(platform.osEnvironment());
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (!RegExp(r'^[A-Za-z_][A-Za-z0-9_]*$').hasMatch(key)) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = platform.osEnvironment()['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

bool _isSafeDotenvPath(String path) {
  if (path.isEmpty || path.contains('\0') || path.contains('..')) {
    return false;
  }
  if (path.startsWith('/') || RegExp(r'^[A-Za-z]:[\\/]').hasMatch(path)) {
    return false;
  }
  final segments = path.split(RegExp(r'[\\/]'));
  if (segments.contains('..')) {
    return false;
  }
  final base = segments.isEmpty ? '' : segments.last;
  return base == '.env' || base.startsWith('.env.');
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.where(_isSafeDotenvPath).fold<Map<String, String>>({}, (acc, path) {
    final text = platform.readFileUtf8(path);
    if (text != null) {
      acc.addAll(parseDotenv(text));
    }
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final configDir = pick(['CONFIG_DIR'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (configDir != null) out['CONFIG_DIR'] = configDir;
  final gatewayUrl = pick(['HAPPY_WAKEY_GATEWAY_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (gatewayUrl != null) out['HAPPY_WAKEY_GATEWAY_URL'] = gatewayUrl;
  final gitRepoPath = pick(['GIT_REPO_PATH'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (gitRepoPath != null) out['GIT_REPO_PATH'] = gitRepoPath;
  final happyWakeyOauthPort = pick(['HAPPY_WAKEY_OAUTH_PORT'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (happyWakeyOauthPort != null) out['HAPPY_WAKEY_OAUTH_PORT'] = happyWakeyOauthPort;
  final openMeteoBaseUrl = pick(['OPEN_METEO_BASE_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'https://api.open-meteo.com/v1/forecast');
  if (openMeteoBaseUrl != null) out['OPEN_METEO_BASE_URL'] = openMeteoBaseUrl;
  final platformUrl = pick(['HAPPY_WAKEY_PLATFORM_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (platformUrl != null) out['HAPPY_WAKEY_PLATFORM_URL'] = platformUrl;
  final sharedAuthUrl = pick(['HAPPY_WAKEY_SHARED_AUTH_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (sharedAuthUrl != null) out['HAPPY_WAKEY_SHARED_AUTH_URL'] = sharedAuthUrl;
  final supabaseUrl = pick(['SUPABASE_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'https://vgzyyfhnendriyrhakkp.supabase.co');
  if (supabaseUrl != null) out['SUPABASE_URL'] = supabaseUrl;
  final contract = checkOsEnv(out);
  if (contract.isNotEmpty) {
    throw MissingEnv(name: contract.first.path, expectedType: 'json-schema-2020-12', examples: const <String>[]);
  }
  return out;
}

const List<String> _dotenvFiles = [];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(platform.osEnvironment(), loadDotenvFiles(_dotenvFiles));
}

class ContractError {
  const ContractError({required this.path, required this.message});
  final String path;
  final String message;
  @override
  String toString() => '$path: $message';
}

/// Validate the resolved env map against the generated JSON Schema rules.
List<ContractError> checkOsEnv(Map<String, String> env) {
  final errors = <ContractError>[];
  {
    final raw = _nonEmpty(env['CONFIG_DIR']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'CONFIG_DIR', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['HAPPY_WAKEY_GATEWAY_URL']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'HAPPY_WAKEY_GATEWAY_URL', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['GIT_REPO_PATH']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'GIT_REPO_PATH', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['HAPPY_WAKEY_OAUTH_PORT']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'HAPPY_WAKEY_OAUTH_PORT', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['OPEN_METEO_BASE_URL']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'OPEN_METEO_BASE_URL', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['HAPPY_WAKEY_PLATFORM_URL']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'HAPPY_WAKEY_PLATFORM_URL', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['HAPPY_WAKEY_SHARED_AUTH_URL']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'HAPPY_WAKEY_SHARED_AUTH_URL', message: message));
    }
  }
  {
    final raw = _nonEmpty(env['SUPABASE_URL']);
    if (raw != null) {
      final message = _contractCheckString(raw);
      if (message != null) errors.add(ContractError(path: 'SUPABASE_URL', message: message));
    }
  }
  for (final key in env.keys) {
    if (!_knownEnvKeys.contains(key)) {
      errors.add(ContractError(path: key, message: 'additional property not in the env contract'));
    }
  }
  return errors;
}

void assertOsEnv(Map<String, String> env) {
  final errors = checkOsEnv(env);
  if (errors.isNotEmpty) {
    throw StateError('environment contract violated: ${errors.join('; ')}');
  }
}

const _knownEnvKeys = <String>{'CONFIG_DIR', 'HAPPY_WAKEY_GATEWAY_URL', 'GIT_REPO_PATH', 'HAPPY_WAKEY_OAUTH_PORT', 'OPEN_METEO_BASE_URL', 'HAPPY_WAKEY_PLATFORM_URL', 'HAPPY_WAKEY_SHARED_AUTH_URL', 'SUPABASE_URL', };

String? _contractCheckString(String raw) => raw.isEmpty ? 'empty string' : null;

/// Like `loadFrom`, but throws when a required key is missing or empty.
DesktopEnvValues tryLoadFrom(String? Function(String key) lookup) {
  final values = loadFrom(lookup);
  return values;
}

DesktopEnvValues tryLoadFromOs() {
  return tryLoadFrom((key) => platform.osEnvironment()[key]);
}
